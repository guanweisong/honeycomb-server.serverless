datasource db {
  provider = "mongodb"
  url      = env("MONGODB_URI")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        String     @id @default(auto()) @map("_id") @db.ObjectId
  createdAt DateTime   @default(now()) @map("created_at") @db.Date
  updatedAt DateTime   @updatedAt @map("updated_at") @db.Date
  email     String     @unique()
  level     UserLevel  @default(EDITOR)
  name      String     @unique()
  password  String
  status    UserStatus @default(ENABLE)
  pages     Page[]
  posts     Post[]
}

model Category {
  id          String         @id @default(auto()) @map("_id") @db.ObjectId
  description String
  parent      String?        @db.ObjectId
  status      CategoryStatus @default(ENABLE)
  title       String
  titleEn     String
  createdAt   DateTime       @default(now()) @map("created_at") @db.Date
  updatedAt   DateTime       @updatedAt @map("updated_at") @db.Date
  posts       Post[]
}

model Comment {
  id        String        @id @default(auto()) @map("_id") @db.ObjectId
  userAgent String        @map("user_agent")
  author    String
  content   String
  email     String
  ip        String
  parentId  String?       @map("parent_id") @db.ObjectId
  postId    String        @map("post_id") @db.ObjectId
  status    CommentStatus @default(PUBLISH)
  createdAt DateTime      @default(now()) @map("created_at") @db.Date
  updatedAt DateTime      @updatedAt @map("updated_at") @db.Date
}

model Link {
  id          String     @id @default(auto()) @map("_id") @db.ObjectId
  url         String     @unique()
  name        String
  logo        String
  description String
  status      LinkStatus @default(ENABLE)
  createdAt   DateTime   @default(now()) @map("created_at") @db.Date
  updatedAt   DateTime   @updatedAt @map("updated_at") @db.Date
}

model Media {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  createdAt DateTime @default(now()) @map("created_at") @db.Date
  key       String
  name      String
  size      Int
  type      String
  url       String
  updatedAt DateTime @updatedAt @map("updated_at") @db.Date
  postCover Post[]   @relation("cover")
  height    Int?
  width     Int?
}

model Menu {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  createdAt DateTime @default(now()) @map("created_at") @db.Date
  parent    String?  @db.ObjectId
  power     Int
  type      MenuType
  updatedAt DateTime @updatedAt @map("updated_at") @db.Date
}

model Page {
  id        String     @id @default(auto()) @map("_id") @db.ObjectId
  createdAt DateTime   @default(now()) @map("created_at") @db.Date
  authorId  String     @map("author_id") @db.ObjectId
  author    User       @relation(fields: [authorId], references: [id])
  content   String
  status    PageStatus @default(TO_AUDIT)
  title     String
  views     Int        @default(0)
  updatedAt DateTime   @updatedAt @map("updated_at") @db.Date
}

model Post {
  id               String            @id @default(auto()) @map("_id") @db.ObjectId
  commentStatus    PostCommentStatus @default(ENABLE) @map("comment_status")
  createdAt        DateTime          @default(now()) @map("created_at") @db.Date
  galleryLocation  String?           @map("gallery_location")
  galleryStyleIds  String[]          @map("gallery_style_ids") @db.ObjectId
  galleryTime      DateTime?         @map("gallery_time") @db.Date
  movieActorIds    String[]          @map("movie_actor_ids") @db.ObjectId
  movieDirectorIds String[]          @map("movie_director_ids") @db.ObjectId
  movieNameEn      String?           @map("movie_name_en")
  movieStyleIds    String[]          @map("movie_style_ids") @db.ObjectId
  movieTime        DateTime?         @map("movie_time") @db.Date
  authorId         String            @map("author_id") @db.ObjectId
  author           User              @relation(fields: [authorId], references: [id])
  categoryId       String            @map("category_id") @db.ObjectId
  category         Category          @relation(fields: [categoryId], references: [id])
  content          String?
  coverId          String?           @map("cover_id") @db.ObjectId
  cover            Media?            @relation("cover", fields: [coverId], references: [id])
  excerpt          String?
  status           PostStatus        @default(TO_AUDIT)
  title            String?
  type             PostType          @default(ARTICLE)
  views            Int               @default(0)
  quoteAuthor      String?           @map("quote_author")
  quoteContent     String?           @map("quote_content")
  updatedAt        DateTime          @updatedAt @map("updated_at") @db.Date
}

model Setting {
  id            String   @id @default(auto()) @map("_id") @db.ObjectId
  siteCopyright String?  @map("site_copyright")
  siteName      String   @map("site_name")
  siteRecordNo  String?  @map("site_record_no")
  siteRecordUrl String?  @map("site_record_url")
  siteSignature String?  @map("site_signature")
  siteSubName   String?  @map("site_sub_name")
  updatedAt     DateTime @updatedAt @map("updated_at") @db.Date
}

model Tag {
  id                 String   @id @default(auto()) @map("_id") @db.ObjectId
  createdAt          DateTime @default(now()) @map("created_at") @db.Date
  name               String   @unique()
  updatedAt          DateTime @updatedAt @map("updated_at") @db.Date
}

model Token {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  createdAt DateTime @default(now()) @map("created_at") @db.Date
  content   String   @unique()
  updatedAt DateTime @updatedAt @map("updated_at") @db.Date
  userId    String   @db.ObjectId
}

enum UserLevel {
  ADMIN
  EDITOR
  GUEST
}

enum UserStatus {
  DELETED
  ENABLE
  DISABLE
}

enum CategoryStatus {
  ENABLE
  DISABLE
}

enum CommentStatus {
  TO_AUDIT
  PUBLISH
  RUBBISH
  BAN
}

enum LinkStatus {
  ENABLE
  DISABLE
}

enum PageStatus {
  PUBLISHED
  DRAFT
  TO_AUDIT
}

enum PostStatus {
  PUBLISHED
  DRAFT
  TO_AUDIT
}

enum PostType {
  ARTICLE
  MOVIE
  PHOTOGRAPH
  QUOTE
}

enum MenuType {
  CATEGORY
  PAGE
}

enum PostCommentStatus {
  ENABLE
  DISABLE
}
